# ====================================
# Zeta 测试环境配置
# ====================================
# 使用方法: cp .env.test.example .env.test
# 修改以下配置为实际测试环境值

# ------------------------------------
# 数据库配置
# ------------------------------------
POSTGRES_USER=zeta_test
POSTGRES_PASSWORD=zeta-test-password-123
POSTGRES_DB=zeta_test

# ------------------------------------
# Redis 配置
# ------------------------------------
REDIS_PASSWORD=zeta-test-redis-123

# ------------------------------------
# JWT 配置 (测试环境可以用简单密钥)
# ------------------------------------
JWT_SECRET=test-jwt-secret-key-for-testing-environment
JWT_REFRESH_SECRET=test-refresh-secret-key-for-testing

# ------------------------------------
# 前端 URL (测试环境地址)
# ------------------------------------
# 本地测试: http://localhost:8000
# 服务器测试: http://test.your-domain.com 或 https://test.your-domain.com
FRONTEND_URL=http://localhost:8000

# ------------------------------------
# OAuth - GitHub (测试环境可使用同一个应用)
# ------------------------------------
# 在 GitHub OAuth 应用设置中添加测试环境回调 URL
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_CALLBACK_URL=http://localhost:8000/auth/callback/github

# ------------------------------------
# OAuth - DingTalk (测试环境)
# ------------------------------------
DINGTALK_APP_KEY=your-dingtalk-app-key
DINGTALK_APP_SECRET=your-dingtalk-app-secret
DINGTALK_CALLBACK_URL=http://localhost:8000/auth/callback/dingtalk

# ------------------------------------
# 邮件服务 (测试环境)
# ------------------------------------
# 测试环境可使用真实邮箱或使用测试 SMTP 服务
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USER=your-email@example.com
MAIL_PASSWORD=your-email-password
MAIL_FROM="Zeta Test <noreply@zeta.test>"

# ------------------------------------
# 七牛云对象存储 (测试环境)
# ------------------------------------
# 测试环境可使用与生产相同的配置，或申请测试空间
QINIU_ACCESS_KEY=your-qiniu-access-key
QINIU_SECRET_KEY=your-qiniu-secret-key
QINIU_BUCKET=your-test-bucket-name
QINIU_DOMAIN=https://test-domain.com
QINIU_ZONE=z0
